{{#each countries as |country|}}
  <i class="flag-md flag-md-{{country.isoCode}} {{if (not-eq country selectedCountry) "opacity-30"}}" style={{if (eq country.isoCode "GB-ENG") "background:black;"}} {{action (mut selectedCountry) country}}></i>
{{/each}}
