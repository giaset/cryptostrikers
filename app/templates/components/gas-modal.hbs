<div class="modal-dialog modal-dialog-centered" role="document">
  <div class="modal-content">
    <div class="modal-header">
      <h5 class="modal-title" id="gasModalLabel">Transaction Info</h5>
      <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    </div>
    <div class="modal-body">
      <p>In order to process this transaction, MetaMask requires payment of a gas price. The gas price will help determine how quickly this transaction is mined and recorded to the blockchain.</p>
      <p>Please select your desired transaction speed from the options below.</p>
    </div>
    <div class="modal-footer">
      {{#each gasOptions as |gasOption|}}
        <button type="button" class="btn btn-primary" {{action "confirmButtonClicked" gasOption.gasPrice}}><span class="font-weight-bold">{{gasOption.name}}</span><br><small>{{gasOption.gasPrice}} Gwei</small></button>
      {{/each}}
    </div>
  </div>
</div>
